export interface CharacterResponse {
  id: string;
  name: string;
  species: string;
  gender: string;
  image: string;
}

export interface CharacterID {
  ID: string;
}

export interface CharacterName {
  NAME: string;
}